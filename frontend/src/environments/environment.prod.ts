// Production environment — points to Render backend
// Replace YOUR_RENDER_APP_NAME with your actual Render service name
export const environment = {
  production: true,
  apiBaseUrl: 'https://YOUR_RENDER_APP_NAME.onrender.com'
};
